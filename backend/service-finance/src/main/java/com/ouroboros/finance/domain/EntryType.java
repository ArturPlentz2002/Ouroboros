package com.ouroboros.finance.domain;

/** Natureza de um lancamento financeiro. */
public enum EntryType {
  /** Saida de dinheiro (gasto). */
  EXPENSE,
  /** Entrada de dinheiro (receita). */
  INCOME
}
