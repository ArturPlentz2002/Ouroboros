{
  "name": "ouroboros",
  "version": "0.0.0",
  "private": true,
  "description": "Tooling de qualidade de commits (commitlint + husky) do monorepo Ouroboros",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  }
}
